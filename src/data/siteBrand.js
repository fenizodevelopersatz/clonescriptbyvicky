export const siteBrand = {
  name: 'CloneScript',
  companyName: 'CloneScript',
  logo: '/logo.webp',
  // Dark navy wordmark on the default logo disappears on a black header --
  // this white/blue-glow variant is for routes that fill the header black
  // (see hasDarkTopHeader in Header.jsx).
  logoDark: '/blue_logo.webp',
  // Same asset, named for the other place it's used on its own merits
  // rather than as a dark-header fallback (the schedule-free-demo
  // social-proof panel, which asks for the blue wordmark specifically and
  // supplies its own dark chip behind it -- see .sfd-logo-chip).
  logoBlue: '/blue_logo.webp',
  logoWidth: 2048,
  logoHeight: 768,
}
