# Local-only, gitignored (see ../.gitignore's generic .env pattern).
# These are the credentials that used to be hardcoded directly in sendMail.php.
# That old value is already committed in git history (commit 9b27c0f) and
# pushed to origin -- rotate the password on the Hostinger account and paste
# the NEW one below. This file only removes the leak going forward.
SMTP_HOST=smtp.hostinger.com
SMTP_USERNAME=enquirie@fenizomlmsoft.com
SMTP_PASSWORD=tquRBj4~
SMTP_PORT=465
RECIPIENT_EMAIL=phptestingtls@gmail.com
#RECIPIENT_EMAIL=fenizotechnologies@gmail.com
RECIPIENT_NAME=CloneScript Team
